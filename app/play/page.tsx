import { SoloWorlds } from "@/components/play/SoloWorlds";

// The global solo world-book. Per-project solo play lives at /project/[title]/play.
export default function PlayPage() {
  return <SoloWorlds />;
}
